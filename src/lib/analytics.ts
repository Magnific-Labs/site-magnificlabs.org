/**
 * Google Analytics 4 measurement id from the Firebase web app registered in
 * project `site-magnificlabs-org`.
 *
 * This is not a secret — it is designed to ship in client code and identifies
 * the destination property, not the sender. Events land in the Analytics
 * section of the Firebase console.
 */
export const MEASUREMENT_ID = 'G-D7Z32REH9S'

/** Host serving gtag.js, worth preconnecting to. */
export const ANALYTICS_ORIGIN = 'https://www.googletagmanager.com'
